version: "3"
services:
  db:
    image:  postgres
    restart: always 
    ports:
      - "5433:5432"
    environment:
       POSTGRES_PASSWORD: 12345678

# // Start containers in detached / background mode
# docker-compose up -d

# // Stop containers
# docker-compose down